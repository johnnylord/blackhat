@parent styleguide.objects 0
@page objreadme README

The `object/` folder contains all the stylesheets of objects.
Object is **independent** of the **context**, and there is **no other objects under the object itself**.
